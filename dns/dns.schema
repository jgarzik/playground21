CREATE TABLE domains (
	name		TEXT PRIMARY KEY
);

CREATE TABLE hosts (
	name		TEXT NOT NULL,
	rec_type	TEXT NOT NULL,
	value		TEXT NOT NULL,
	owner_pubkey	TEXT NOT NULL
);

CREATE INDEX hosts_idx ON hosts (name);

